/public/
/resources/_gen/
.hugo_build.lock
.DS_Store
.idea/

# Work in progress — papers and drafts held back from the public site.
# Nothing in here is committed, and Hugo never sees it (it is outside content/).
/parkinglot/

# Working notes, not deliverables. This repository is public and serves omnibachi.org;
# a session handoff and a working-instructions file are neither published content nor
# something a visitor should find. Kept locally only.
/SOTU.md
/CLAUDE.md

# Generated by doc/build_manuscript.py. The authored sources are
# doc/ase_autonomy_without_authority_draft.md and doc/ase_tables.md; the script composes them
# with the figures and tables into these, and renders the PNGs from the authored SVGs. All are
# rebuilt on every run and none is ever edited by hand.
/doc/ase_manuscript.md
/doc/ase_manuscript.docx
/doc/figures/png/
