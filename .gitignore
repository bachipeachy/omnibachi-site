/public/
/resources/_gen/
.hugo_build.lock
.DS_Store
.idea/

# Work in progress — papers and drafts held back from the public site.
# Nothing in here is committed, and Hugo never sees it (it is outside content/).
/parkinglot/

# Working notes, not deliverables. This repository is public and serves omnibachi.org;
# a session handoff and a working-instructions file are neither published content nor
# something a visitor should find. Kept locally only.
/SOTU.md
/CLAUDE.md

# Generated by ase_doc/build_manuscript.py. The authored sources are
# ase_doc/ase_autonomy_without_authority_draft.md and ase_doc/ase_tables.md; the script composes
# them with the figures and tables into these, and renders the PNGs from the authored SVGs. All
# are rebuilt on every run and none is ever edited by hand.
/ase_doc/ase_manuscript.md
/ase_doc/ase_manuscript.docx
/ase_doc/figures/png/

# Publisher manuscript material — submitted originals, cover letters, and review
# copies. None of it is published: the publisher clears the paper for the site,
# not the correspondence. What goes public is content/papers/ and static/figures/;
# a PDF meant to be kept belongs in parkinglot/pdf_published/.
/ieee_doc/
/jss_doc/
/wiley_doc/
/ase_doc/
/oxford_doc/
